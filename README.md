# Modeling Development of Number Concepts

```
> git clone https://github.com/riadas/numerosity.git
> cd numerosity
> julia
julia> ] activate .
pkg> instantiate
julia> include("metalanguage/didactic/plot_base_result.jl") # OR: open one of the jupyter notebooks
```
